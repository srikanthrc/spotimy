# Spotify API Configuration
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Optional: Pre-existing Spotify tokens (if you have them)
# These will be obtained automatically via OAuth if not provided
# SPOTIFY_USER_ACCESS_TOKEN=your_access_token_here
# SPOTIFY_REFRESH_TOKEN=your_refresh_token_here
# SPOTIFY_AUTH_CODE=your_auth_code_here
# SPOTIFY_TOKEN_EXPIRES_AT=token_expiry_timestamp

# Server Configuration
HTTP_HOST=0.0.0.0
HTTP_PORT=3001

# Ngrok Configuration (optional but recommended)
# Get your auth token from https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTH_TOKEN=your_ngrok_auth_token_here

# Optional: Use a custom ngrok domain (requires paid ngrok plan)
# NGROK_DOMAIN=your-custom-domain.ngrok.dev